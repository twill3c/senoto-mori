# TEST_SPEC.md — senoto-mori

<!-- scaffold template v1.12.0 から展開(2026-08-27) -->

## 実行規約

- `npm test`(vitest)を stage 3–5 の判定に使う。テストは `src/**/*.test.ts` と `tests/**/*.test.ts`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 合成データに解析解を期待する場合、期待値の導出前提をテスト内の assert で検算し、
  導出過程をコメントに残す。前提を検算しない期待値は正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威 | 出典 URL と取得日をデータファイルの先頭に書く |
| 実測 | 実測日と実測値をコメントに残す |

件数は原則、定数でなく**不変量**(集合の一致・保存則・双方向の含意)で書く。
ただし本プロジェクトの区画数・料金は**架空の設定を SPEC §2 で正本として固定した**ため、
SPEC §2 の値との一致を定数で検査してよい。これは外部データではなく自分で決めた仕様である。

## オラクルの出所

| フィクスチャ / 期待値 | 出所 | 性格 |
|---|---|---|
| 敷地・渓流・森林のポリゴン | 自作(架空の地形) | 幾何計算の入力 |
| `riverside` / `meadow` フラグ | **人手で記述** | 幾何から独立した第二の記述。G-03 で照合する |
| 甲府の日最低気温平年値(12 か月) | 気象庁 1991–2020 平年値・2026-08-27 取得 | 外部権威 |
| 料金の素朴実装(`naiveQuote`) | テスト内に別実装 | G-04 の照合相手 |
| 開場日数 227 | SPEC §2(4/18–11/30)から算出 | 仕様 |
| 祝日一覧 | 内閣府「国民の祝日」CSV(`data/syukujitsu.csv`)・2026-08-27 取得 | 外部権威(原本を vendor。要約は使わない) |

**循環の禁止**: `riverside` / `meadow` を座標から生成してはならない。生成した瞬間に G-03 は
何も検証しなくなる。フラグは区画を置いた人間の意図として手で書き、幾何は座標から独立に計算する。

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | SPEC §2.1 | 区画総数 | 40 |
| T-002 | SPEC §2.1 | タイプ別内訳 | seoto 8 / komore 12 / hidamari 10 / solo 6 / takibi 4 |
| T-003 | SPEC §2.1 | 区画 ID の一意性 | 重複ゼロ |
| T-004 | G-02 | 全区画の四隅が敷地ポリゴン内 | 全件 true |
| T-005 | G-02 | 区画同士が重ならない | 全 780 対で非重複 |
| T-006 | G-03 | `riverside` ⇔ 渓流からの距離 ≤ 30 m(**双方向**) | 一致。片側だけの成立を不合格とする |
| T-007 | G-03 | `meadow` ⇔ 森林ポリゴンの外(**双方向**) | 一致 |
| T-008 | SPEC §2.1 | `openFire` が真なのは takibi のみ | 4 件 |
| T-009 | SPEC §2.1 | `ac` が真なのは komore のみ・8 件 | 8 件 |
| T-010 | SPEC §2.1 | seoto は全件 riverside かつ非 meadow | 8 件 |
| T-011 | SPEC §2.1 | hidamari は全件 meadow かつ非 riverside | 10 件 |
| T-020 | G-06 | 開場境界 4/17 不可・4/18 可・11/30 可・12/1 不可 | SPEC §2 |
| T-021 | G-06 | 2026 年の開場日数 | 227 |
| T-022 | G-06 | 閉場期間の代表日(1/1・3/1・12/25)が不可 | false |
| T-030 | G-04 | 繁忙期判定: GW/お盆/紅葉期の境界日 | SPEC §2.2 |
| T-031 | G-04 | 土曜・祝前日は繁忙期 | 土曜の代表日で true |
| T-032 | G-04 | **料金の二実装照合**: 5 タイプ × 開場全日 × 大人 1–6 名 | 本実装 = 素朴実装 |
| T-033 | SPEC §2.2 | 未就学児は無料・大人換算 6 名超は拒否 | エラー |
| T-034 | SPEC §2.2 | AC 加算は komore のみ受け付ける | 他タイプで指定するとエラー |
| T-040 | F-07 | 気温は「甲府平年値 − 設定差分」で導出される | 全 12 か月で一致 |
| T-041 | F-07 | 早見表は開場期間の 8 か月のみを返す | 4–11 月 |
| T-050 | F-14 / G-08 | フッタリンク 5 件・並び・全 https | [[fleet-footer-standard]] |
| T-060 | G-07 | ナビと各ページのパス集合が一致(内部リンクの 404 ゼロ) | 一致 |
| T-070 | G-09 | `src/components` `src/app` に生の色リテラルが無い | 0 件 |
| T-030b | G-04 | 祝前日は繁忙期。理由は「祝前日」 | 期間外かつ土曜でない祝前日で成立 |
| T-071 | G-11 | `src` `tests` `docs` と主要 md にキリル文字・ハングルが無い | 0 件 |

### L2 で追加(場内図)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-090 | F-01 | 投影: 原点が左下・北ほど上・東ほど右・川の上流が上 | 座標の大小で縛る |
| T-090b | F-01 | 縮尺が等方。全頂点と全区画が viewBox に収まる | 歪み無し・はみ出し無し |
| T-100 | **G-01** | 描画結果の `data-site-id` 集合 = `PITCHES` の ID 集合 | 過不足ゼロ・重複ゼロ |
| T-101 | F-01 | 地形 4 種(敷地・森・草原・渓流)が描かれる | id で確認 |
| T-102 | F-01 | 施設が全件、注記として出る | FACILITIES 全件 |
| T-103 | F-01 | 図が読み上げ可能(role / aria-labelledby / 区画ごとの title) | 描画結果で確認 |
| T-104 | F-02 | 絞り込み 6 種が区画データの述語と一致。どれも全件でも 0 件でもない | 一致 |
| T-104b | F-02 | 絞り込みは AND。両立しない組は 0 件 | 直火可 × 横付けは空 |
| T-106 | F-02 | 該当しない区画に `data-dim="true"` が付く | 全 40 区画で確認 |
| T-107 | F-03 | 状態を渡すと `data-status` に出る。渡さなければ unknown | 一致 |
| T-110 | **G-05** | 各日の 空 + 満 = 40。タイプ別の総和も一致 | 開場 227 日すべて |
| T-111 | G-05 | 決定論 — 同じ日を二度引いて同じ | 一致 |
| T-112 | G-05 | 常に満・常に空の区画が無い | 227 日を通して 0 件 |
| T-113 | G-05 | 繁忙期の充填率が通常期を 15 ポイント超上回る。両群の標本数を先に確認 | 需要の向き |
| T-116 | F-03 | タイプ別ラベル: 0 なら満、総数の 25 %(最低 1)以下なら残僅 | 表示上の決めごと |

### L3 で追加(予約とオフシーズン)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-130 | F-08 | 季節の三態(開場前 / 開場中 / 今季終了)と次の開場日 | SPEC §2 |
| T-131 | F-08 | 閉場中は「終わったこと」と「次に開くこと」の両方を出し、**予約への導線を出さない** | 描画結果で確認 |
| T-140 | F-09 | 閉場日・人数上限・AC・焚火学舎の受講確認・名前・メール・泊数の検証 | SPEC §2.1 / §2.2 |
| T-140c | F-09 | 最終泊が閉場日にかかる申し込みを拒否(11/30 に 2 泊) | 拒否 |
| T-140i | F-09 | エラーは日本語で、どの項目かが分かる。zod の既定英語を出さない | 全 issue で確認 |
| T-141 | F-09 | 見積もりの各泊が `quote()` と一致し、内訳の合計が総額に一致する | 料金の正本はひとつ |
| T-150 | F-09 | 月の枠・空き数・曜日オフセット・月送りの端 | dayAvailability と一致 |
| T-150d | F-09 | 開場期間の全日がどこかの月に一度だけ現れる | 227 日 |

## 未実装(後続ループ)

| ID | 対応要求 | ループ |
|---|---|---|
| T-160 | F-15 / F-16 / F-17(OG 画像・JSON-LD・sitemap) | L4 |
| T-170 | N-01(Lighthouse 95+) | L4 |

## 生成物に対するゲート(G-10)

`npm run build && npm run verify` で `scripts/verify-output.mjs` が `.next/server/app` の
HTML 全件を読み、フッタの個数・5 項目の並び・架空注記の存在を検査する。
ソースを読む検査では、React が補間値の前後に挟むコメントや、条件分岐で消えた要素を捉えられない。
**出荷される形そのものを読む**(HC-016 の 2026-08-23 追記)。

L2 で **G-01 の出荷版**を追加した。`/stay` の HTML に載っている `data-site-id` と、
`src/data/sites.ts` の実テキストから正規表現で拾った区画 ID を突き合わせる。
TS の型を通さない別の道からデータを取ることで、二つの独立した記述の照合になる。
あわせて地形 4 種と、JS 無しでも読める区画表の存在を確認する(N-06)。

**このゲートが噛むことは実証済み**(loop_002): 出荷 HTML の `HI-07` を `HI-99` に書き換えたところ、
「図に無い区画」と「データに無い ID」の両方向を報告して exit 1 になった。
落ちることを確かめていないゲートは、落ちないゲートである(HC-036)。

L3 で **N-02 の出荷版(G-12)** を追加した。`/reserve` の全入力欄に対応する `label` があること、
タブ順を乱す `tabindex` が無いこと、`aria-describedby` の参照先が実在すること、
submit ボタンと「予約は成立しません」の注記があることを検査する。

これも噛むことを実証済み: `label` の `for` を壊し `tabindex="3"` を差し込んだところ、
両方を報告して exit 1 になった。**この実証がなければ危なかった** —— 追加した直後のゲートは
正規表現の単語境界(バックスラッシュ + b)がバックスペース文字に化けていて何にも一致せず、
「入力欄が 0 個」という別の失敗として現れていた(loop_003 / HC-028 の射程外)。

**この節を含め、バックスラッシュを含む文字列をシェル経由で書かないこと。**
ヒアドキュメントでも Python の非 raw 文字列でも一段落ちる。編集ツールで直接書くか、
文字コードから組み立てる(HC-028)。

