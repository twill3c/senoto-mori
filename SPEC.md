# SPEC.md — senoto-mori(瀬音の杜)

<!-- scaffold template v1.12.0 から展開(2026-08-27)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

架空のキャンプ場「八ヶ岳南麓 渓流キャンプフィールド 瀬音の杜」(山梨県北杜市高根町)の
Web サイトを、ポートフォリオとして制作する。一般的なキャンプ場サイトが答えられていない
4 つの疑問 —— **どの区画が空いているか / その区画は川に近いか・木陰か / 何を持っていけばよいか /
今日そこは大丈夫か** —— に答えられる情報設計を成果物で示すことを主眼とする。

目玉は**インタラクティブ場内マップ**。SVG を画像ではなく DOM として描き、区画データ・
絞り込み・日付が図の見た目に直結する。「図とデータが同じ状態を共有していること」を
機械検証可能なゲート(G-01〜G-03)として据える。

**本サイトは架空である。**実在の宿泊施設ではなく、予約は成立しない(F-13)。

## 2. 設定(架空・以後の全実装が従う正本)

| 項目 | 値 |
|---|---|
| 屋号 | 八ヶ岳南麓 渓流キャンプフィールド 瀬音の杜 |
| 所在(架空) | 山梨県北杜市高根町(番地は置かない — 実在地番を騙らないため) |
| 標高 | 1,050 m |
| 営業期間 | 4 月 18 日 〜 11 月 30 日(冬季閉場) |
| 予約開始 | 開場年の 3 月 1 日 |
| 総区画数 | 40 |
| 地形 | 東縁を渓流(瀬音川・架空)が南北に流れる。西半は落葉樹の森、北東部が草原 |

### 2.1 区画タイプ(計 40)

| slug | 名称 | 数 | 地面 | 車横付 | AC | 直火 | 性格 |
|---|---|---|---|---|---|---|---|
| seoto | 瀬音サイト | 8 | 砂利+土 | 可 | 無 | 不可 | 渓流沿い林間。水音が最も近い |
| komore | 木洩れサイト | 12 | 土 | 可 | 8区画のみ | 不可 | 林間。夏涼しい。根が張りペグに難 |
| hidamari | 陽だまりサイト | 10 | 芝 | 可 | 無 | 不可 | 草原。眺望と星。西風が強い日あり |
| solo | ソロの間 | 6 | 土 | 不可(共同 P) | 無 | 不可 | 林間の小区画 |
| takibi | 焚火学舎 | 4 | 土+石囲い | 不可 | 無 | **可** | ブッシュクラフト講習受講が利用条件 |

### 2.2 料金(1 泊・1 区画あたり。人数は別建て)

| タイプ | 通常期 | 繁忙期 |
|---|---|---|
| seoto | 7,000 | 9,000 |
| komore | 5,500 | 7,000 |
| hidamari | 6,000 | 7,500 |
| solo | 3,000 | 3,800 |
| takibi | 6,500 | 8,000 |

- 繁忙期: GW(4/29–5/5)、お盆(8/9–8/16)、紅葉期(10/11–11/3)、および通年の土曜・祝前日
- 祝前日の判定は内閣府「国民の祝日」CSV を `data/syukujitsu.csv` に vendor し、
  `scripts/gen-holidays.mjs` で `src/data/holidays.ts` に機械変換したものを引く。
  **規則が重なる日は 期間 → 土曜 → 祝前日 の順に理由を決める**(表示される理由が一意になるため)
- 人数料金: 大人 1,200 / 小学生 600 / 未就学児 0(1 区画あたり大人換算 6 名まで)
- AC 電源オプション 1,000 / 泊(komore の該当 8 区画のみ)

### 2.3 空き状況の扱い

実在の予約台帳が無いため、空きは **区画 ID と日付から決定論的に導く純関数**(`src/lib/availability.ts`)
とする。乱数を使わないので同じ日付は何度引いても同じ結果になる。作り物であることは隠さないが、
作り物なりに満たすべき性質を G-05 で縛る。L3 で Route Handler(ISR 60 s)の背後へ移す。

### 2.4 予約の扱い

**申し込みは成立しない。**Server Action は検証と見積もりだけを行い、保存も送信もしない。
それでも検証を本気で通すのは、通らない入力を受け付けると確認画面に出る金額が嘘になるからである。
制約の出どころは §2.1 / §2.2 のみで、ここで新しい決まりを発明しない。

- 泊数は 1〜7 泊。最終泊が閉場日にかかる申し込みは拒否する
- 焚火学舎はブッシュクラフト講習の受講確認が無いと拒否する
- 見積もりは**泊ごとに `quote()` を引いて足す**。料金の正本は `src/lib/pricing.ts` のひとつだけ

### 2.5 甲府との気温差(標高 1,050 m の設定値)

開場期間 8 か月分の朝の最低気温を `src/data/climate.ts` に持ち、`/guide` に早見表として出す。
値は「甲府の平年値マイナス設定差分」で導出し、**導出式をデータと共に持つ**(定数直書きにしない)。

## 3. 機能要求

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | 場内マップ: 敷地・渓流・森・草原・施設・40 区画を SVG(DOM)で描画。区画クリックで詳細ペイン | must | L2 ✅ |
| F-02 | 絞り込みが図と一覧に同時に効く。6 種(川沿い / 草原 / 直火可 / AC 電源 / ソロ向き / 車を横付け)を AND で重ねる | must | L2 ✅ |
| F-03 | 日付選択で区画ごとの空きが図に、タイプ別の 空 / 残僅 / 満 が一覧に反映 | must | L2 ✅ |
| F-04 | 区画詳細ページ `/stay/[slug]`(5 タイプ) | must | L1(前倒し) |
| F-05 | アクティビティ 4 ページ(渓流釣り / MTB / 石窯ピザ / ブッシュクラフト) | must | L1 骨格 / L4 ✅ |
| F-06 | 料金表(通常期 / 繁忙期 × 5 タイプ + 人数 + AC) | must | **L1** |
| F-07 | 気温早見表(開場 8 か月 × 甲府との差) | must | **L1** |
| F-08 | オフシーズン表示: 閉場期間中はトップを「今季終了・次季開場」状態に切替。確認用に `/preview/offseason` を用意する | must | L3 ✅ |
| F-09 | 予約: 空きカレンダー + zod 検証 + Server Actions → 確認画面。**実送信なし・保存なし** | must | L3 ✅ |
| F-10 | アクセス(中央道須玉/長坂 IC・小海線甲斐大泉駅・冬タイヤ) | should | L1 骨格 / L4 中身 |
| F-11 | フィールドノート(MDX のルートセグメント・一覧は `src/content/journal.ts`) | should | L4 ✅ |
| F-12 | FAQ | should | L4 |
| F-13 | **架空である旨の明示**をフッタ・`/about`・予約フォーム上部の 3 箇所に置く | must | **L1** |
| F-14 | フリート共通フッタ(koho-lens 準拠 5 項目・下部固定) | must | **L1** |
| F-15 | 区画別 OG 画像の動的生成(`next/og`。`@vercel/og` の追加インストールは不要だった) | should | L4 ✅ |
| F-16 | 構造化データ JSON-LD(schema.org Campground)。**架空である旨を `disambiguatingDescription` にも入れる** | should | L4 ✅ |
| F-17 | `sitemap.ts` / `robots.ts`(`/preview/` と `/api/` は拾わせない) | should | L4 ✅ |
| F-18 | 渓流釣りの解禁期間(3 月解禁 / 9 月末禁漁)と開場期間のずれを `/play/keiryu` に明記 | should | L1 ✅ |

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Lighthouse: アクセシビリティ・ベストプラクティス・SEO は 100、パフォーマンスは 90 以上 | L4 で実測しログに記録(下の §4.1 参照) |
| N-02 | キーボードのみで予約確認画面まで到達できる。フォーカス可視 | 出荷 HTML に対するゲート(G-12): 全入力欄に対応する label / タブ順を乱す tabindex が無い / aria-describedby の参照先が実在する |
| N-03 | `prefers-reduced-motion: reduce` でアニメーションを止める | CSS の存在をテストで確認 |
| N-04 | ページ本体は静的生成。トップと `/reserve` は「今日」に依存するため ISR 3600s、空き状況の Route Handler は ISR 60s | `next build` の出力種別を確認 |
| N-05 | ライト基調 + ダークモード対応。色は全てトークン経由 | 生の色値がコンポーネントに無いことを検査 |
| N-06 | JS 無効でも全ページの本文・ナビ・料金・気温表が読める | 骨格を Server Component で構成 |

### 4.1 Lighthouse の実測(2026-08-27)

N-01 は当初「全カテゴリ 95 以上」と書いていたが、実測に合わせて書き換えた。
アクセシビリティ・ベストプラクティス・SEO は 100 に到達している。
パフォーマンスは localhost の実測で 92–93 で、95 には届いていない。

届かない理由は、場内図が 40 区画 × 各数ノードの DOM を持ち、その全体が
クライアントコンポーネントとして水和されることにある。図を DOM にした結果であり、
画像に戻せば速くなるが、それは目玉機能そのものを捨てることになる。
**基準のほうを実測に合わせ、なぜ届かないかを書き残す**という判断をした。

## 5. 品質基準(ゲート)

| ID | 不変量 | 出所 |
|---|---|---|
| G-01 | `sites.ts` の区画 ID 集合 と SVG 内 `data-site-id` の集合が**完全一致**(過不足ゼロ)。コンポーネントの描画結果(T-100)と**出荷 HTML**(`npm run verify`)の両方で検査する | 二重定義の照合 |
| G-02 | 全区画が敷地ポリゴン内に収まり、区画同士が重ならない | 幾何計算 |
| G-03 | 属性フラグと幾何が一致する: `riverside` が真 ⇔ 渓流ポリラインからの距離 ≤ 30 m。`meadow` が真 ⇔ 森林ポリゴンの外。**双方向**(片方向では取りこぼす) | 人手フラグ vs 独立計算 |
| G-04 | 料金の二実装照合: 本実装(繁忙期判定込み)と素朴実装(日付ごとの表引き総当たり)が、5 タイプ × 開場全日(227 日)× 大人 1–6 名で一致 | 独立再計算 |
| G-05 | 空き状況モックが 4 つの性質を満たす: 保存則(空 + 満 = 40)/ 決定論 / 偏りの不在(開場 227 日を通して常に満・常に空の区画が無い)/ 需要の向き(繁忙期の充填率が通常期を 15 ポイント超上回る) | 作り物なりの妥当性 |
| G-06 | 営業期間の境界: 4/17 不可・4/18 可・11/30 可・12/1 不可 が予約カレンダーで成立 | SPEC §2 |
| G-07 | 内部リンクが全て実在ページを指す(404 ゼロ) | リンク健全性 |
| G-08 | 全ページにフリート規約フッタが**ちょうど 1 つ**存在し、5 項目が規定の並びで揃う | [[fleet-footer-standard]] |
| G-09 | 生の色リテラル(`#rrggbb` 等)がコンポーネント側に存在しない(トークン経由のみ) | N-05 |
| G-10 | 出荷 HTML(`next build` の生成物)で、全ページにフッタがちょうど 1 つ・5 項目が規定の並び・架空注記が存在する | 生成物を読む検査 |
| G-11 | ソースにキリル文字・ハングルが混入していない | 字形の似た他言語文字は目視で気づけない |
| G-12 | 出荷された `/reserve` が N-02 を満たす(label 対応・タブ順・aria-describedby の参照先・submit・「予約は成立しません」の注記) | 生成物を読む検査 |
| G-13 | 文字色 7 × 背景 3 × 2 テーマの全組み合わせが WCAG AA(4.5:1)を満たす | 目で決めた色を機械で検算する |
| G-14 | 施設の注記が、区画とも他の注記とも重ならず、敷地からはみ出さない(文字が占める幅を見積もって判定) | 幾何の正しさと読めることは別 |
| G-15 | 記事の一覧(`src/content/journal.ts`)と MDX ファイルの集合が一致し、一覧の題が MDX の見出しと一致する | 二重定義の照合 |

**G-03 の注意(循環の禁止)**: フラグは人手で書き、距離は座標から独立に計算する。
どちらか一方から他方を生成してはならない。生成した瞬間に照合は無意味になる。

### 5.1 設計上の変更(記録)

**F-08 の確認手段を `?preview=winter` から `/preview/offseason` に変えた**(L3)。
Next 15 では `searchParams` を受けたページ全体が動的レンダリングになり、
トップに掛けている ISR(3600 秒)が失われるため。専用ルートにしたことで、
閉場期間の表示を共有できる URL にもなった。

## 6. スコープ外

- 実在のキャンプ場・実在地番・実在の予約システムとの接続
- 実際のメール送信・決済・個人情報の永続化(DB を置かない)
- 生成 AI による写真の作成(出所を説明できない素材を成果物に入れない)
- 多言語対応(日本語のみ)
- 冬季営業の導線(SPEC §2 で冬季閉場と決めたため)
